# DGFM (Dimension-Guided Flow Matching) — requirements.txt
# --- Core compute ---
# Install the right CUDA/CPU wheel yourself first. RTX 50-series / Blackwell
# GPUs require a CUDA 12.8+ or CUDA 13.0 PyTorch wheel; for example:
#   pip install torch==2.12.1+cu130 --index-url https://download.pytorch.org/whl/cu130
torch>=2.7
numpy>=1.26,<3.0
scipy>=1.11,<2.0

# --- Training / utils ---
scikit-learn>=1.3,<1.8   # PCA / IncrementalPCA used in data processing
tqdm>=4.66,<5.0
easydict>=1.10,<2.0
einops>=0.7,<1.0
annoy==1.17.3

# --- Data I/O ---
h5py>=3.9,<4.0
pyyaml>=6.0,<7.0

# --- Robotics & simulation ---
# robosuite 1.5 targets the official 'mujoco' package (not mujoco-py).
robosuite==1.5.1
mujoco>=3.3,<3.4         # tested on 3.3.4
gymnasium==1.1.1

# --- Optimal transport (imported as `ot`) ---
POT==0.9.5

# --- Rendering / video ---
matplotlib>=3.7,<3.11    # tested around 3.10.x
imageio>=2.28,<3.0
imageio-ffmpeg>=0.4.9,<0.7
opencv-python>=4.8,<5.0
